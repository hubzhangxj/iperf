iperf:
  iperf_bw_sin_1:
    category: 'Performance network bandwidth bw_sin_1'
    scores_way: 'compute_speed_score 1'
    parser: 'iperf_TCP_parser'
  iperf_bw_sin_4:
    category: 'Performance network bandwidth bw_sin_4'
    scores_way: 'compute_speed_score 1'
    parser: 'iperf_UDP_parser'
