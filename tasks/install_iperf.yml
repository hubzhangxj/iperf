- name: copy file
  copy: src=iperf.tar.gz dest=/tmp/iperf.tar.gz mode=0777

- name: tar file
  shell: cd /tmp && tar xvf iperf.tar.gz && rm -rf iperf.tar.gz

- name: run install
  shell: "cd /tmp/iperf && ./install.sh"
  ignore_errors: yes
